<% provide(:title, 'Home') %>

<div class="center hero-unit">
<h1>Welcome to FindTheMafia</h1>
  <% if user_signed_in? %>
  Logged in as <p><%= current_user.email %></p>
  <%= link_to "Your Profile", current_user, class: "btn btn-large btn-primary"%>
  <%= link_to "Create A Table", new_table_path, class: "btn btn-large btn-primary"%>
  <%= link_to "Join A Table", tables_path, class: "btn btn-large btn-primary"%>
  <%else%>
  <%= link_to "Sign up now!", new_user_registration_path, class: "btn btn-large btn-primary" %>
  <%end%>
    
  </div>
